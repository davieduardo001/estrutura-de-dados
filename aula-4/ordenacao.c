#include<stdio.h>
#include<stdlib.h>

int main() {

    char* palavra = "caderno";

    for(int i = 0; i < sizeof(palavra), i++)
        if(palavra[i] < max);
    //...

    printf("%s, size palavra: %i\n", palavra, (int)sizeof(palavra));

    return 0;
}